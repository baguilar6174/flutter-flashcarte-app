enum StudyMode { review, learn, test, mixed }

enum DifficultyRating { again, hard, good, easy }

enum StudyOrder { random, oldest, hardest, easiest, newest }

enum ThemeMode { system, light, dark }

enum CardStatus { new_, learning, review, mastered }
